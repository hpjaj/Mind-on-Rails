require 'rails_helper'

describe "flashcards" do

  it "cannot be created by an anonymous user" do
  end

  context "for a logged in standard user" do
    it "can make their note a flashcard" do
    end

    it "can make another user's public note a flashcard of theirs" do
    end

    it "can view all their flashcards in one view" do
    end

    it "cannot view another users flashcards" do
    end
    
  end


end